package main


func main() {
    // tap.Open()
    // t2s := tap.Open()
    // chSignal := make(chan os.Signal)
    // signal.Notify(chSignal, os.Interrupt, syscall.SIGTERM)
    // <-chSignal
    // t2s.Close()
}
